#include <iostream>
#include <vector>
#include <unordered_set>
#include <numeric>

using NodeID = int;
using Set = std::vector<int>;

int main() {
    int P = 67;
    // Just mock paths. Wait, too hard to mock 67 paths.
    return 0;
}
